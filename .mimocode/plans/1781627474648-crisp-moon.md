# Plan: Adaptive Height Modal with Scrolling Content

## Summary
Make the shortcuts modal height adapt to screen size (max 90vh) and enable scrolling for the shortcut list when content overflows.

## Changes

### File: `src/app/components/shortcuts-overlay/shortcuts-overlay.component.html`

**Line 7:** Add `max-h-[90vh] overflow-hidden` to modal container:
```html
<div class="w-full max-w-md max-h-[90vh] rounded-2xl p-6 shadow-2xl overflow-hidden ..."
```

**Line 24:** Add `overflow-y-auto` to the shortcuts list container:
```html
<div class="space-y-2 overflow-y-auto">
```

**Line 41-43:** Move footer note outside scrollable area (optional) — or keep inside if preferred.

## Verification
1. Run `npm run tauri dev`
2. Open shortcuts overlay with F1 or Ctrl+/
3. Modal should not exceed 90vh
4. Shortcut list should scroll when content exceeds available space
